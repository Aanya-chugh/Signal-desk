// Fill in YOUR Supabase values. BOTH are safe to expose publicly (anon key only).
// Find them in Supabase: Project Settings -> API.
window.SB_URL = "https://YOUR-PROJECT-ref.supabase.co";  // Project URL
window.SB_KEY = "YOUR-SUPABASE-ANON-PUBLIC-KEY";          // anon public key (NOT service_role)
