// No client-side keys anymore.
// The dashboard now reads data through the secured /api/news endpoint, so the
// Supabase key stays server-side only and is never exposed in the browser.
// This file is intentionally empty (kept so any old reference won't 404).
